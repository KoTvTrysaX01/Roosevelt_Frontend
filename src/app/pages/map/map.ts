import { Component } from '@angular/core';
import { Maxbox } from "../../components/mapbox/mapbox";

@Component({
  selector: 'app-map',
  imports: [Maxbox],
  templateUrl: './map.html',
  styleUrl: './map.scss',
})
export class Map {

}
